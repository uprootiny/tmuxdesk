# hyle — primary creative/ops node
set -g @host_sigil "🜂"
set -g @host_name "hyle"

# Deskfloor integration (hyle-specific, degrades if not installed)
set -ga status-right ' #[fg=colour244]│ #[fg=colour82]#(~/deskfloor/bin/deskfloor-status 2>/dev/null)'

# TPM + resurrect + continuum are configured in tmux.base.conf
